/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: "class", // Enable dark mode based on a class (usually 'dark' on the root element)
  content: [
    "./src/**/*.{html,js,jsx,ts,tsx}", // Add the path where your source files are located
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
